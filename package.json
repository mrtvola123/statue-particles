{
  "name": "statue-particles",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "build": "vite build",
    "test": "vitest run --coverage",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "three": "^0.179.1",
    "vite": "^7.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.0",
    "@vitest/coverage-v8": "^3.2.0",
    "vitest": "^3.2.0"
  }
}
